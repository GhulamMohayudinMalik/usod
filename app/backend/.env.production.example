# Production Environment Configuration Template
# Copy this to .env.production and fill in actual values
# NEVER commit production .env files to version control

# ========================================
# Environment
# ========================================
NODE_ENV=production

# ========================================
# Database (MongoDB Atlas or AWS DocumentDB)
# ========================================
# MongoDB Atlas example:
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/usod?retryWrites=true&w=majority

# AWS DocumentDB example:
# MONGODB_URI=mongodb://username:password@docdb-cluster.region.docdb.amazonaws.com:27017/usod?tls=true&replicaSet=rs0&readPreference=secondaryPreferred&retryWrites=false

# ========================================
# Server Configuration
# ========================================
PORT=5000

# ========================================
# Security (GENERATE NEW SECURE VALUES!)
# ========================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=REPLACE_WITH_64_CHAR_RANDOM_STRING

# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
INGEST_API_KEY=REPLACE_WITH_32_CHAR_RANDOM_STRING

# ========================================
# Frontend Configuration
# ========================================
# Your production frontend URL (must use HTTPS)
FRONTEND_URL=https://your-frontend-domain.com

# Desktop app URL (if applicable)
DESKTOP_APP_URL=https://desktop.your-domain.com

# Additional allowed origins (comma-separated)
ALLOWED_ORIGINS=https://app.yourdomain.com,https://admin.yourdomain.com

# ========================================
# AWS Configuration (if using S3 for backups)
# ========================================
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_S3_BUCKET=usod-backups

# ========================================
# Backup Configuration
# ========================================
BACKUP_DIR=./backups
MAX_BACKUPS=10
BACKUP_RETENTION_DAYS=30

# ========================================
# Monitoring & Logging (optional)
# ========================================
# LOG_LEVEL=info
# SENTRY_DSN=your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-new-relic-key
